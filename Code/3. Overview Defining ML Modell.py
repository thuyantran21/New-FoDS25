########################################################################################################
################################### Overview of 4. Defining best ML Modell #############################
########################################################################################################
'''
4.1 contains:
    # Code ranking different ML Models (1st to 12th Place)
    # Defining top 4 best ML Models (saved as top_4_ML)
    # Model Comparison Plot by F1-Scores
    # For each metric output contains F1-Score, Accuracy, Precision, Recall, ROC-AUC to each model
    # Learning Curves comparing the top 4 ML Models (for each metric 1 plot with 4 graphs)
    # Training Loss Curves 


4.2 contains:
    # Learning Curves for all metrics, but to specific ML Models (e.g., all targets using Logistic Reg.)

4.3 contains:
    # 
    # 

# 4.4 contains:
    # 
    # 
    # 



'''


########################################################################################################
################################### Comments (Possible Changes/Improvements) ###########################
########################################################################################################
''' 
4.1 Defines best ML Models:
    -> Initially, we have chosen Logistic Regression as fourth ML Model
    -> but using the code, the top 4 ML models contain Gradient Boosting, instead of Log. Regr.
    -> So it might give sense, to use GradientBoosting instead, as it would allow to:
        - save top_4_ML
        - and then run the further code automatically with the right Models
    
    Needs to be discussed in group!
    What does Thuy's Cousin thinks? Better the 4 best models or the top 3 + one of the worse ones?
    
    After deciding on final ML Model Selection:
    -> if GradientBoosting was chosen:      update 4.2
    -> if Logistic Regression was chosen:   update 4.1 
    
'''

